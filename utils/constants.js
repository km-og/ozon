const progress = document.querySelector(".progress");
const progressInputValue = progress.querySelector(".progress__input-value");
const progressCircle = progress.querySelector(".progress__circle");
const progressBtnAnimate = progress.querySelector(".progress__animate");
const progressBtnHide = progress.querySelector(".progress__hide");
const inputAnimated = document.getElementById("progress-animated");

export {
  progress,
  progressInputValue,
  progressCircle,
  progressBtnAnimate,
  progressBtnHide,
  inputAnimated,
};
